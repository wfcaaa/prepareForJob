^代表行开头，\转义  *重复匹配  $代表行尾 d删除 \s是空白匹配  *代表出现0次或者多次 "^$" 代表空行!

1.删除空行的方法:
grep -v '^$' file  // -v 选择不符合表达式的行 
sed '/^$/d'  file 或 sed -n '/./p' file
awk '/./ {print}' file 或 awk '{if($0!=" ") print}'
tr -s "n"
也可以在vim中输入 "%s/^\n//g" 
如果要删除文本中的多个空行只保留一行空行 可以在vim中输入 "%s/^\n$//g"
